

class PreferenceRepository {
//  static const _USER_ID = "ID";
//  static const _USER_NAME = "NAME";
//
//
//  Observable<void> saveUser(User domain.user) {
//    return _preference.flatMap((pref) => Observable.concat(List.unmodifiable(
//        [pref.setString(_USER_ID, user.id), pref.setString(_USER_NAME, user.name)])));
//  }

//  Observable<User> getUser() {
//    return _preference.flatMap((pref) {
//      var id = pref.getString(_USER_ID);
//      var name = pref.getString(_USER_NAME);
//      return id != null && name != null ? Observable.just(User(id, name)) : Observable.empty();
//    });
//  }
}
